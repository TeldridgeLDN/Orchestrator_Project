---
// A/B Test Modal Component for Standard Package
---

<div id="ab-modal" class="ab-modal hidden">
  <div class="ab-modal-overlay" onclick="document.getElementById('ab-modal').classList.add('hidden')"></div>
  <div class="ab-modal-content">
    <button class="ab-modal-close" onclick="document.getElementById('ab-modal').classList.add('hidden')">
      ✕
    </button>
    
    <h2>🧪 Headline A/B Test Results</h2>
    <p class="ab-modal-intro">
      We tested 3 headline variants with 500 visitors over 5 days. Here's what we learned:
    </p>

    <div class="variants">
      <!-- Variant A - Feature-Focused (Lost) -->
      <div class="variant-card variant-lost">
        <div class="variant-header">
          <h3>Variant A: Feature-Focused</h3>
          <span class="variant-result lost">❌ Lost</span>
        </div>
        <p class="variant-headline">"Premium Peat-Free Soil Blends for Houseplants"</p>
        
        <div class="conversion-metric">
          <div class="conversion-bar-container">
            <div class="conversion-bar" style="width: 37.5%; background: #E07A5F;">
              <span class="bar-label">4.2%</span>
            </div>
          </div>
          <p class="conversion-stats">21 signups from 500 visitors</p>
        </div>
        
        <div class="analysis">
          <strong>Why it lost:</strong>
          <p>Generic, feature-driven language doesn't address customer pain points or desired outcomes.</p>
        </div>
      </div>

      <!-- Variant B - Pain-Focused (Lost) -->
      <div class="variant-card variant-lost">
        <div class="variant-header">
          <h3>Variant B: Pain-Focused</h3>
          <span class="variant-result almost">⚠️ Close</span>
        </div>
        <p class="variant-headline">"Stop Killing Your Plants. Start With Better Soil."</p>
        
        <div class="conversion-metric">
          <div class="conversion-bar-container">
            <div class="conversion-bar" style="width: 77.7%; background: #87A96B;">
              <span class="bar-label">8.7%</span>
            </div>
          </div>
          <p class="conversion-stats">44 signups from 500 visitors</p>
        </div>
        
        <div class="analysis">
          <strong>Why it lost:</strong>
          <p>Strong attention-grabber, but "killing" may be slightly aggressive. Some customers felt blamed.</p>
        </div>
      </div>

      <!-- Variant C - Benefit-Focused (WINNER) -->
      <div class="variant-card variant-winner">
        <div class="variant-header">
          <h3>Variant C: Benefit-Focused</h3>
          <span class="variant-result winner">✅ WINNER</span>
        </div>
        <p class="variant-headline">"Thriving Houseplants Start With Better Soil"</p>
        
        <div class="conversion-metric">
          <div class="conversion-bar-container">
            <div class="conversion-bar winner-bar" style="width: 100%; background: #2C5F2D;">
              <span class="bar-label">11.2%</span>
            </div>
          </div>
          <p class="conversion-stats">56 signups from 500 visitors</p>
        </div>
        
        <div class="analysis winner-analysis">
          <strong>Why it won:</strong>
          <p>Positive framing creates aspiration. "Thriving" paints a vivid picture of success. Gentle nudge without blame.</p>
        </div>
      </div>
    </div>

    <!-- Key Insight -->
    <div class="insight-box">
      <h4>💡 Key Insight</h4>
      <p>
        <strong>Benefit-focused headlines outperformed pain-focused by 28.7%</strong> and feature-focused by <strong>166.7%</strong>.
      </p>
      <p class="insight-detail">
        Customers respond better to positive aspirational messaging than problem-focused language, especially in lifestyle categories like plants.
      </p>
    </div>

    <!-- Timeline -->
    <div class="testing-timeline">
      <h4>Testing Timeline</h4>
      <div class="timeline-steps">
        <div class="timeline-step">
          <div class="step-number">1</div>
          <div class="step-content">
            <strong>Hour 0-6</strong>
            <p>Built page + 3 variants</p>
          </div>
        </div>
        <div class="timeline-arrow">→</div>
        <div class="timeline-step">
          <div class="step-number">2</div>
          <div class="step-content">
            <strong>Day 1-5</strong>
            <p>Ran split test (500 visitors)</p>
          </div>
        </div>
        <div class="timeline-arrow">→</div>
        <div class="timeline-step">
          <div class="step-number">3</div>
          <div class="step-content">
            <strong>Hour 114-120</strong>
            <p>Analyzed & deployed winner</p>
          </div>
        </div>
      </div>
      <p class="timeline-summary">
        <strong>Total time:</strong> 48 hours from concept to optimized launch
      </p>
    </div>

    <!-- This is what validation looks like -->
    <div class="validation-callout">
      <p>
        <strong>This is what validation looks like.</strong> We don't just design—we test, measure, and optimize based on real user data.
      </p>
    </div>
  </div>
</div>

<button class="ab-test-trigger" onclick="document.getElementById('ab-modal').classList.remove('hidden')">
  🧪 View A/B Test Results
</button>

<style>
  .ab-test-trigger {
    position: absolute;
    top: 5rem;
    right: var(--spacing-md);
    background: rgba(255, 255, 255, 0.95);
    border: 2px solid var(--color-forest-green);
    color: var(--color-forest-green);
    padding: var(--spacing-sm) var(--spacing-md);
    border-radius: var(--radius-lg);
    font-weight: 600;
    cursor: pointer;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
    z-index: 999;
  }

  .ab-test-trigger:hover {
    background: var(--color-forest-green);
    color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .ab-modal {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-md);
    transition: opacity 0.3s ease;
  }

  .ab-modal.hidden {
    display: none;
  }

  .ab-modal-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
  }

  .ab-modal-content {
    position: relative;
    background: white;
    border-radius: var(--radius-lg);
    max-width: 900px;
    max-height: 90vh;
    overflow-y: auto;
    padding: var(--spacing-2xl);
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
  }

  .ab-modal-close {
    position: absolute;
    top: var(--spacing-md);
    right: var(--spacing-md);
    background: none;
    border: none;
    font-size: 1.5rem;
    color: var(--color-light-text);
    cursor: pointer;
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: all 0.2s ease;
  }

  .ab-modal-close:hover {
    background: var(--color-off-white);
    color: var(--color-dark-text);
  }

  .ab-modal-content h2 {
    color: var(--color-forest-green);
    margin-bottom: var(--spacing-sm);
  }

  .ab-modal-intro {
    color: var(--color-light-text);
    margin-bottom: var(--spacing-xl);
    font-size: 1.05rem;
  }

  .variants {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
  }

  .variant-card {
    border: 2px solid #e0e0e0;
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    transition: all 0.3s ease;
  }

  .variant-card.variant-winner {
    border-color: var(--color-forest-green);
    background: linear-gradient(135deg, #f1f8f1 0%, white 100%);
  }

  .variant-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-sm);
  }

  .variant-header h3 {
    font-size: 1.125rem;
    margin: 0;
  }

  .variant-result {
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: var(--radius-sm);
    font-size: 0.875rem;
    font-weight: 700;
  }

  .variant-result.lost {
    background: #ffe0e0;
    color: #c62828;
  }

  .variant-result.almost {
    background: #fff4e0;
    color: #f57c00;
  }

  .variant-result.winner {
    background: #e8f5e9;
    color: var(--color-forest-green);
  }

  .variant-headline {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-dark-text);
    margin: var(--spacing-md) 0;
    padding: var(--spacing-sm);
    background: var(--color-off-white);
    border-radius: var(--radius-sm);
  }

  .conversion-metric {
    margin: var(--spacing-md) 0;
  }

  .conversion-bar-container {
    height: 2.5rem;
    background: #f0f0f0;
    border-radius: var(--radius-md);
    overflow: hidden;
    margin-bottom: var(--spacing-xs);
  }

  .conversion-bar {
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    padding-right: var(--spacing-sm);
    transition: width 0.5s ease;
  }

  .bar-label {
    color: white;
    font-weight: 700;
    font-size: 1.125rem;
  }

  .winner-bar {
    animation: fillBar 1s ease-out;
  }

  @keyframes fillBar {
    from { width: 0; }
    to { width: 100%; }
  }

  .conversion-stats {
    font-size: 0.875rem;
    color: var(--color-light-text);
    margin: 0;
  }

  .analysis {
    background: #f9f9f9;
    padding: var(--spacing-sm);
    border-radius: var(--radius-sm);
    margin-top: var(--spacing-sm);
  }

  .analysis strong {
    color: var(--color-dark-text);
    display: block;
    margin-bottom: var(--spacing-xs);
  }

  .analysis p {
    color: var(--color-light-text);
    font-size: 0.9rem;
    margin: 0;
  }

  .winner-analysis {
    background: #e8f5e9;
  }

  .insight-box {
    background: linear-gradient(135deg, var(--color-forest-green), #1e4620);
    color: white;
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    margin-bottom: var(--spacing-xl);
  }

  .insight-box h4 {
    color: white;
    margin-bottom: var(--spacing-sm);
  }

  .insight-box p {
    color: white;
    margin-bottom: var(--spacing-sm);
  }

  .insight-box p:last-child {
    margin-bottom: 0;
  }

  .insight-detail {
    font-size: 0.9rem;
    opacity: 0.9;
  }

  .testing-timeline {
    background: var(--color-off-white);
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    margin-bottom: var(--spacing-xl);
  }

  .testing-timeline h4 {
    margin-bottom: var(--spacing-md);
  }

  .timeline-steps {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
  }

  .timeline-step {
    flex: 1;
    display: flex;
    gap: var(--spacing-sm);
  }

  .step-number {
    width: 2rem;
    height: 2rem;
    background: var(--color-forest-green);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    flex-shrink: 0;
  }

  .step-content strong {
    display: block;
    color: var(--color-dark-text);
    font-size: 0.9rem;
  }

  .step-content p {
    font-size: 0.85rem;
    color: var(--color-light-text);
    margin: 0;
  }

  .timeline-arrow {
    color: var(--color-forest-green);
    font-size: 1.5rem;
    font-weight: 700;
  }

  .timeline-summary {
    text-align: center;
    font-size: 1rem;
    color: var(--color-light-text);
    margin: var(--spacing-md) 0 0;
  }

  .validation-callout {
    background: var(--color-terracotta);
    color: white;
    padding: var(--spacing-lg);
    border-radius: var(--radius-md);
    text-align: center;
  }

  .validation-callout p {
    color: white;
    font-size: 1.125rem;
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .ab-test-trigger {
      top: auto;
      bottom: var(--spacing-md);
      right: var(--spacing-sm);
      font-size: 0.875rem;
      padding: var(--spacing-xs) var(--spacing-sm);
    }

    .ab-modal-content {
      padding: var(--spacing-lg);
    }

    .timeline-steps {
      flex-direction: column;
    }

    .timeline-arrow {
      transform: rotate(90deg);
    }

    .variant-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--spacing-xs);
    }
  }
</style>

